apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: modex-data
  namespace: usd-djha
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  storageClassName: rook-ceph-block
